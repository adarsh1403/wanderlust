<nav class="bg-white border-b border-zinc-200 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            <!-- Left: Brand -->
            <div class="flex-shrink-0 flex items-center">
                <a href="/listings" class="text-xl font-bold text-zinc-900 flex items-center gap-1">
                    Driftr<span class="text-orange-700">•</span>
                </a>
            </div>

            <!-- Center: Search Form -->
            <div class="hidden md:flex flex-1 justify-center px-2 absolute left-1/2 transform -translate-x-1/2">
                <form action="/listings" method="GET" class="flex items-center border border-zinc-200 rounded-full pl-4 pr-1 py-1 shadow-sm w-72 bg-white">
                    <input class="w-full text-sm text-zinc-900 focus:outline-none bg-transparent" type="search" placeholder="Search destinations" aria-label="Search" name="q">
                    <button class="bg-orange-700 text-white rounded-full px-4 py-2 text-sm hover:bg-orange-800 transition-colors" type="submit">
                        <i class="fa-solid fa-magnifying-glass"></i>
                    </button>
                </form>
            </div>

            <!-- Right: Links -->
            <div class="hidden md:flex items-center space-x-6">
                <a class="text-sm text-zinc-700 hover:text-zinc-900 font-medium" href="/listings/new">Host with Driftr</a>
                <% if (!currentUser) { %>
                    <a class="text-sm text-zinc-700 hover:text-zinc-900 font-medium" href="/login">Login</a>
                    <a class="text-sm text-zinc-700 hover:text-zinc-900 font-medium" href="/signup">Signup</a>
                <% } else { %>
                    <a class="text-sm text-zinc-700 hover:text-zinc-900 font-medium" href="/logout">Logout</a>
                <% } %>
            </div>

            <!-- Mobile menu button -->
            <div class="md:hidden flex items-center">
                <button type="button" id="mobile-menu-btn" class="text-zinc-500 hover:text-zinc-900 focus:outline-none">
                    <i class="fa-solid fa-bars text-xl"></i>
                </button>
            </div>
        </div>
    </div>

    <!-- Mobile menu -->
    <div class="md:hidden hidden" id="mobile-menu">
        <div class="px-4 pt-2 pb-4 space-y-2 border-t border-zinc-100 bg-white shadow-sm">
            <form action="/listings" method="GET" class="flex items-center border border-zinc-200 rounded-full pl-4 pr-1 py-1 mb-4 shadow-sm">
                <input class="w-full text-sm text-zinc-900 focus:outline-none bg-transparent" type="search" placeholder="Search destinations" name="q">
                <button class="bg-orange-700 text-white rounded-full px-4 py-2 text-sm hover:bg-orange-800 transition-colors" type="submit">
                    <i class="fa-solid fa-magnifying-glass"></i>
                </button>
            </form>
            <a class="block text-sm text-zinc-700 hover:text-zinc-900 font-medium py-2" href="/listings/new">Host with Driftr</a>
            <% if (!currentUser) { %>
                <a class="block text-sm text-zinc-700 hover:text-zinc-900 font-medium py-2" href="/login">Login</a>
                <a class="block text-sm text-zinc-700 hover:text-zinc-900 font-medium py-2" href="/signup">Signup</a>
            <% } else { %>
                <a class="block text-sm text-zinc-700 hover:text-zinc-900 font-medium py-2" href="/logout">Logout</a>
            <% } %>
        </div>
    </div>
</nav>

<script>
    document.getElementById('mobile-menu-btn').addEventListener('click', function() {
        document.getElementById('mobile-menu').classList.toggle('hidden');
    });
</script>